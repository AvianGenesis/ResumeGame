using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Atk01 : MonoBehaviour
{
    /*
    public AttackClass atkInfo;
    public AttackClass nextNorm, nextWait;
    public Animation anim;

    private Vector4 hitbox;
    private string moveName;
    private float startup;
    private float recovery;
    private float walkTime;

    void Start()
    {
        moveName = "attack01WIP";
        startup = 0.25f;
        recovery = 0.5f;
        walkTime = 0.5f;

        atkInfo = new AttackClass
        {
            nextNorm = nextNorm,
            nextWait = nextWait,
            anim = anim,
            hitbox = new Vector4(1.0f, 0.0f, 0.5f, 0.5f),
            moveName = "attack01WIP",
            startup = 0.25f,
            recovery = 0.5f,
            walkTime = 0.5f
        };
    }
    */
}
